export type UserTypes = {
  username: string;
  email: string;
  role: string;
  password: string;
};

export type UserLoginTypes = {
  sub: number;
  firstName: string;
  lastName: string;
  username: string;
  email: string;
  role: string;
}

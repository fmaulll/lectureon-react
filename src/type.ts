export type UserTypes = {
  username: string;
  email: string;
  role: string;
  password: string;
};

export type UserLoginTypes = {
  username: string;
  email: string;
  role: string;
  access_token: string;
  refresh_token: string;
}

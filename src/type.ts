export type UserTypes = {
  username: string;
  email: string;
  role: string;
  password: string;
};

export type UserLoginTypes = {
  id: number;
  username: string;
  email: string;
  role: string;
}
